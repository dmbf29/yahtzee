<div class="container">
  <h1><%= @game.name ? @game.name : "Yahtzee" %></h1>

  <p>Participants</p>
  <ol>
    <%= @game.users.each do |user| %>
      <li><%= user.name %></li>
    <% end %>
  </ol>

</div>
